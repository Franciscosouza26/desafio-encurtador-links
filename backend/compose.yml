services:
  dbservice:
    image: mysql:latest
    container_name: encurtadordb
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: encurtador
    volumes:
      - encurtador_data:/varqlib/mysql

volumes:
    encurtador_data: